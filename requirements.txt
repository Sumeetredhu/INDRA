# INDRA runtime dependencies.
#
# Layered deliberately: everything above "OPTIONAL" is required for the system to boot and serve.
# Everything below degrades gracefully when absent (see CLAUDE.md rule 6) — a missing wheel costs
# you a format or a modality, never the process.

# ----- core ---------------------------------------------------------------------------
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.9.0
pydantic-settings>=2.6.0
python-multipart>=0.0.12
httpx>=0.27.0
anyio>=4.6.0
tenacity>=9.0.0
python-dateutil>=2.9.0
orjson>=3.10.0
sse-starlette>=2.1.3

# ----- storage ------------------------------------------------------------------------
neo4j>=5.25.0
chromadb>=0.5.15
sqlalchemy[asyncio]>=2.0.35
aiosqlite>=0.20.0
redis>=5.1.0
numpy>=1.26.0

# ----- llm providers ------------------------------------------------------------------
google-generativeai>=0.8.3
groq>=0.11.0

# ----- document parsing ---------------------------------------------------------------
pypdf>=5.1.0
pdfplumber>=0.11.4
python-docx>=1.1.2
openpyxl>=3.1.5
pandas>=2.2.3
beautifulsoup4>=4.12.3
lxml>=5.3.0
chardet>=5.2.0
python-magic-bin>=0.4.14; sys_platform == "win32"
python-magic>=0.4.27; sys_platform != "win32"

# ----- vision / OCR -------------------------------------------------------------------
pillow>=11.0.0
opencv-python-headless>=4.10.0
pytesseract>=0.3.13
rapidfuzz>=3.10.0

# ----- nlp ----------------------------------------------------------------------------
tiktoken>=0.8.0

# ----- reporting ----------------------------------------------------------------------
reportlab>=4.2.5
pyyaml>=6.0.2

# ======================================================================================
# OPTIONAL — install with `pip install -r requirements-optional.txt`
# ======================================================================================
# openai-whisper          speech-to-text            (mobile agent falls back to text input)
# gtts / pyttsx3          text-to-speech            (mobile agent returns text-only)
# sentence-transformers   local embeddings          (falls back to hash embedder)
# spacy + en_core_web_sm  syntactic relations       (falls back to rule-based extraction)
# ultralytics             YOLOv8 P&ID detection     (rule-based detector is the default, D4)
# easyocr                 alternative OCR engine    (tesseract is the default)
# ollama                  offline local LLM         (falls back to extractive retrieval)
# anthropic               optional chat provider    (D2)
# celery                  distributed task queue    (asyncio background tasks by default)
# asyncpg                 PostgreSQL driver         (SQLite is the default, D10)
